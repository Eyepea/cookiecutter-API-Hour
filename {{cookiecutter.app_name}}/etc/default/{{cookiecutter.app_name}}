# Start the daemon by default, let the user disable it.
START_DAEMON=yes
DAEMON_ARGS="--config_dir=/etc/{{cookiecutter.app_name}} -ac --chdir=/opt/{{cookiecutter.app_name}}  {{cookiecutter.app_name}}:Container"
